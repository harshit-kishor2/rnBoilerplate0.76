export enum UserRoles {
  Guest = 'guest',
  User = 'user',
  Admin = 'admin',
}

export enum LoadingStatus {
  IDLE = 'IDLE',
  PENDING = 'PENDING',
  FULLFILLED = 'FULLFILLED',
  REJECTED = 'REJECTED',
}
